# File overview: This file defines dimensions behavior for runtime configuration and reproducible experiment setup. It records the declarations, settings, fixtures, or guidance needed by that responsibility. Its package-relative location is `config/stage_tutorial/dimensions.conf`.
# dimensions and granularity of length, width and granularity (meters)
200 200 0.3